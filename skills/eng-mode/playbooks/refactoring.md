### Refactoring

1. Read `how`; capture a behavioral baseline or equivalence harness.
2. Name target data shape and reader-load reduction.
3. Use OMP LSP references/rename/rename-file for symbol work.
4. Run `architect` for interface or seam changes. Route behavior changes to Feature.
5. Subtract first. Migrate every caller and delete the old path in one wave. No shim. Read the stack-specific skills `project-standards` names before changing code they govern.
6. Prove equivalence on the real artifact.
7. Run the pre-commit pass `project-standards` names; if delivery includes a PR, run **Opening a PR**. Report contract, target shape, evidence, and reader-load delta.
