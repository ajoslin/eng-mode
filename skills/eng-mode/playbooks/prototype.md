### Prototype

1. Name the decision and competing hypotheses. Otherwise route Feature.
2. Read the `prototype` skill; for visual work use a local `frontend-design` skill only when `project-standards` names one, otherwise Eng Mode's generic design procedure and OMP `browser`.
3. Follow the `prototype` skill's artifact law: clearly marked throwaway code may live beside the target for context, but only on a throwaway branch and never on main. Keep production behavior untouched.
4. Observe deciding evidence with OMP `browser` or a measured executable.
5. Present artifacts, evidence, tradeoffs, and recommendation. Mark throwaway.
6. Route selected direction through `architect` or Feature.
